name: Build & Release

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ========== 单元测试 ==========
  #test:
  #  name: Test (Node ${{ matrix.node }})
  #  runs-on: windows-latest
  #  strategy:
  #    matrix:
  #      node: [18]
  #  steps:
  #    - name: Checkout
  #      uses: actions/checkout@v4
#
  #    - name: Setup Node.js
  #      uses: actions/setup-node@v4
  #      with:
  #        node-version: ${{ matrix.node }}
  #        cache: npm
#
  #    - name: Install Python setuptools (distutils compat for node-gyp)
  #      run: pip install setuptools
#
  #    - name: Install dependencies
  #      run: npm ci
#
  #    - name: Run unit tests
  #      run: npm test

  # ========== 构建 Electron 应用 ==========
  build:
    name: Build (Windows)
    needs: test
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install Python setuptools (distutils compat for node-gyp)
        run: pip install setuptools

      - name: Install dependencies
        run: npm ci

      - name: Build Electron app (Windows)
        run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: build/shortcut-guide-*.exe
          retention-days: 30

  # TODO: macOS 构建（待启用）
  # build-mac:
  #   name: Build (macOS)
  #   needs: test
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 18
  #         cache: npm
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Build Electron app (macOS)
  #       run: npm run build:mac
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: macos-build
  #         path: build/shortcut-guide-*.dmg
  #         retention-days: 30

  # TODO: Linux 构建（待启用）
  # build-linux:
  #   name: Build (Linux)
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 18
  #         cache: npm
  #
  #     - name: Install Linux dependencies
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y libarchive-tools rpm
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Build Electron app (Linux)
  #       run: npm run build:linux
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: linux-build
  #         path: build/shortcut-guide-*.AppImage
  #         retention-days: 30

  # ========== 发布 Release（仅 tag 触发） ==========
  release:
    name: Create Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/windows-build/*
            # artifacts/macos-build/*
            # artifacts/linux-build/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}